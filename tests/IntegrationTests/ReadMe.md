# EndToEnd tests

Uses Aspire.Hosting.Testing and a reference to the Aspire.Apphost project run the application
in a container.

This gives the ability to test the whole application end-to-end.

Test can target any exposed endpoints or pages in the aspire host, e.g.
- API endpoints
- UI pages